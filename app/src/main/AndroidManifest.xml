<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gameloft.pc.quanlythoigian">

    <uses-feature
        android:name="android.hardware.Camera"
        android:required="true" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_app"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".utils.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".utils.ThoiKhoaBieuActivity" />
        <activity android:name=".utils.ThoiGianBieuActivity" />
        <activity android:name=".utils.NoteActivity" />
        <activity android:name=".utils.EditActivity" />
        <activity android:name=".utils.DetailActivity" />
        <activity android:name=".utils.AddActivity" />
        <activity android:name=".utils.EditTgbActivity" />
        <activity android:name=".utils.DetailTgbActivity" />
        <activity android:name=".utils.CameraActivity"></activity>
    </application>

</manifest>